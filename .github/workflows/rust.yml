name: Rust

on:
  push:
    branches: [ "dev", "dev" ]
  pull_request:
    branches: [ "dev", "dev" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Run tests
      run: cargo test --all-features

    - name: Check formatting (optional but recommended)
      run: cargo fmt --all -- --check

    - name: Clippy lints (optional but good practice)
      run: cargo clippy -- -D warnings
